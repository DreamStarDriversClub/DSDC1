{
  "framework": "nextjs",
  "buildCommand": "bunx prisma generate && next build",
  "installCommand": "bun install",
  "outputDirectory": ".next",
  "crons": [
    {
      "path": "/api/health",
      "schedule": "0 */8 * * *"
    }
  ]
}
